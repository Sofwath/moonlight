__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# SQLite databases — the corpus DB can be large
data/*.db
data/*.db-wal
data/*.db-shm

# Benchmark extracted segments (generated by extract_benchmark_segments.py; review before committing)
data/benchmark/main_set/
data/benchmark/calibration_set/*.jsonl

# GeoNames cache (reproduced by `moonlight build-place-names`)
data/geonames/

# Sentence embedding models (downloaded automatically)
models/

# Eval output
data/eval_history.jsonl
docs/EVAL_RESULTS.md

# Environment
.env
*.env
.venv/
venv/

# Generated benchmark results and eval runs (may contain model outputs or cost data)
results/

# Demo video output (raw Playwright recordings excluded; keep only the final MP4)
demo/*
!demo/moonlight_workbench_demo.mp4

# Coverage artefacts
.coverage
htmlcov/

# Generated docs (PDF is binary; regenerate with pandoc)
docs/*.pdf

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
